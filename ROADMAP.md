# Роадмапа NIVO

Один файл — уся черга. Правило: нова ідея йде в «Колись» і не рухається,
поки не з'явиться причина. З «Далі» беремо тільки верхній пункт.

---

## Зараз

- [ ] **`c1-03.json`** — залити разом із виправленим `c1-02.json`, перевірити в `check.html`, бампнути `BANK_VERSION`

---

## Далі

Порядок має значення — беремо зверху.

- [ ] **`c2-03.json`** — 12 нових тем × 3 = +36 питань
      → C2 = 95 питань, ~17% банку, попередження про перекіс зникає
      Теми: reported speech у формальному регістрі, subjunctive у that-clause,
      емфатичне do/did, whereas/whilst, колокації з bearing / stake / scope
- [ ] **Повернення слабких тем** — підмішувати 2–3 питання з `weak_items` у кожні 15
- [ ] **Словниковий режим** — інтервальні повторення, дані з проєкту «Перекладач»
- [ ] **Стрік і денна ціль** — дані вже пишуться в `sessions`
- [ ] **Кнопка встановлення PWA** — без неї на iOS додаток фактично не ставиться
- [ ] **Візуалізація прогресу** — точність за рівнями, скільки до наступного рівня
- [ ] **Вибір сфери** — UI, фільтр пулу, колонка в `profiles`
      Фундамент готовий: поле `domain` є у всіх питаннях
      Правило: мінімум 8 з 15 питань зі сфери, решта добираються з `general`
- [ ] **Монетизація** — домен, Lemon Squeezy
      Не раніше, ніж retention 7-го дня в Umami буде вище 10–15%

---

## Колись

Без зобов'язань. Сюди складаємо ідеї, щоб не тримати в голові.

- Аудіопитання на слух через синтез мовлення
- Пуш-нагадування про денну ціль
- Академічна англійська як окрема сфера (перетинається з категорією `register`)

---

## Зроблено

- **2026-09-21** — `c1-03.json`: +30 питань, C1 = 90, рівно по 3 на тему.
  Типи в C1 тепер: 56 mcq / 15 fill / 19 order. У `c1-02` замінено
  `precedent-02` — майже дослівно повторював `-01`
- **2026-09-15** — `c2-02.json`: +17 питань, C2 = 51, рівно по 3 на тему
- **2026-09-15** — поле `domain` у всіх 487 питаннях; таксономія: `general`,
  `it`, `business`, `travel`, `daily`. Назви зафіксовані — додати легко,
  перейменувати дорого
- **2026-09-15** — `c1-02.json`: +30 питань, C1 = 60, по 2 на тему.
  Типи розбавлено: 17 mcq / 7 fill / 6 order
- **2026-09-15** — у `index.json` виправлено порядок `levels` (A2 стояв після B2),
  додано блок `domains`

---

## Правила, які не змінюємо

- **Глибина, не ширина.** 3 питання на тему. Одне питання на тему означає, що
  повторити тему неможливо — користувач побачить той самий рядок слово в слово
- **Одна тема — один граматичний зміст.** Сфера змінює контекст речення,
  а не тему. Інакше вийде 6 рівнів × 5 сфер = 30 паралельних курикулумів
- **Лексика сфери не йде в скоринг** — `"scored": false`, як у `vocab-01.json`.
  Інакше розробник отримає C1 за знання IT-термінів
- **Старі id не чіпаємо.** Нові питання — нові файли. Прогрес і `weak_items`
  у користувачів лишаються валідними
- **Fill — тільки з однією можливою відповіддю.** Якщо існує синонім,
  що теж підходить, питання стає mcq
