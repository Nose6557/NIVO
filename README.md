# Nivo

Гра-тренажер англійської рівня B1→B2 з відстеженням прогресу.
Інтерфейс і пояснення українською, завдання англійською.

## Як запустити

1. Завантаж усі файли в корінь репозиторію `Nose6557/nivo`
2. Settings → Pages → Source: `Deploy from a branch`, гілка `main`, папка `/ (root)`
3. Через хвилину сайт буде на `https://nose6557.github.io/nivo/`

**Локально** відкривати через `file://` не вийде — браузер заблокує читання `questions.json`.
Запусти простий сервер:
```
python3 -m http.server 8000
```
і відкрий `http://localhost:8000`.

## Supabase

Перед першим запуском виконай SQL зі скрипта в SQL Editor (таблиці + політики RLS).
У `config.js` уже вписані Project URL і publishable-ключ — вони публічні за призначенням.
Секретний ключ (`sb_secret_...`) сюди додавати не можна ніколи.

В Authentication → Providers → Email на час тестування зручно вимкнути «Confirm email».

Для роботи «Забули пароль?» додай адресу сайту (наприклад `https://nose6557.github.io/nivo/`,
для локального запуску — ще й `http://localhost:8000`) в Authentication → URL Configuration →
Redirect URLs. Без цього посилання зі скидання пароля поверне користувача не туди.

## Структура

| Файл | Призначення |
|---|---|
| `index.html` | розмітка чотирьох екранів |
| `style.css` | оформлення |
| `app.js` | логіка гри, рендер завдань, підрахунок |
| `store.js` | шар доступу до даних (Supabase або localStorage) |
| `questions.json` | банк питань — 54 завдання, 8 категорій |
| `config.js` | ключі Supabase |

## Як додавати питання

Дописуй об'єкти в `questions.json`. Формати:

```json
{ "id": "унікальний", "category": "tenses", "item": "ключ правила",
  "type": "mcq", "prompt": "текст з ___", "options": ["a","b","c","d"],
  "answer": "b", "explain": "пояснення українською" }
```

`type` буває три:
- `mcq` — вибір із варіантів
- `fill` — введення слова (`accept` — масив допустимих відповідей)
- `order` — збирання речення з `tokens`

Поле `item` — ключ конкретного правила. Саме за ним рахується статистика слабких місць,
тому однакові правила в різних питаннях мають мати однаковий `item`.

## Трекінг

- Точність по категоріях на головному екрані (шкала рівнів)
- Питання, де ти помиляєшся, з'являються частіше
- Кнопка «Тренувати слабкі місця» — сесія лише з проблемних правил
- «Експорт прогресу» — текстовий звіт, який можна скинути в чат для розбору

## Що далі

- Більше питань (мета — 300+ для повного покриття B2)
- Аудіювання
- Рівень C1
- Підписка через Stripe (потребує серверної частини)
